# EPI Recorder + AGT Example

Export AGT governance evidence into a portable .epi artifact.

## Quickstart

pip install epi-recorder
python agt_to_epi_demo.py

## Output

The demo generates a sample AGT audit export and converts it to .epi.
It prints mapping counts, step kinds, agent names, integrity and signature status.
